This is a twig template.

That has some {{ variables }}.

A WordPress-style gettext call: {{ __('foo', 'bar') }}.

Missing space should be parsed: {{ __('missing space','bar') }}.

Alternative quote-style: {{__("Double quotes", "bar")}}.
